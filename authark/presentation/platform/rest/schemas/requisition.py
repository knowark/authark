# from marshmallow import fields, EXCLUDE
# from .entity import EntitySchema


# class RequisitionSchema(EntitySchema):
    # class Meta:
        # unknown = EXCLUDE

    # tenant = fields.Str(
        # required=True, metadata=dict(example="servagro"))
    # type = fields.Str(required=True, metadata=dict(example="reset"))
    # data = fields.Mapping()
